{
  "name": "recloop-ai-assistant",
  "version": "1.0.0",
  "description": "AI email and calendar scheduling assistant",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:webhook\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev:webhook": "cd webhook-server && npm run dev",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../webhook-server && npm install",
    "start": "pm2 start ecosystem.config.js",
    "stop": "pm2 stop ecosystem.config.js",
    "restart": "pm2 restart ecosystem.config.js",
    "logs": "pm2 logs",
    "deploy": "./scripts/deploy.sh"
  },
  "workspaces": [
    "frontend",
    "backend", 
    "webhook-server"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["ai", "scheduling", "email", "calendar", "gmail", "openai"],
  "author": "Your Name",
  "license": "MIT"
}